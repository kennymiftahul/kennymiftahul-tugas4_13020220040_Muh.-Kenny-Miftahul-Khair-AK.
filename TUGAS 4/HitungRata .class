// Source code is decompiled from a .class file using FernFlower decompiler.
import java.util.ArrayList;
import java.util.ListIterator;
import java.util.Scanner;

public class HitungRata {
   private double total = 0.0;
   private ArrayList nilaiBaru = new ArrayList();
   Scanner input;

   public HitungRata() {
      this.input = new Scanner(System.in);
   }

   public void inputNilai(int[] var1) {
      for(int var2 = 0; var2 < var1.length; ++var2) {
         var1[var2] = this.input.nextInt();
         this.total += (double)var1[var2];
      }

   }

   public double rataNilai(int var1) {
      return this.total / (double)var1;
   }

   public void cetakNilai(int[] var1) {
      int[] var2 = var1;
      int var3 = var1.length;

      for(int var4 = 0; var4 < var3; ++var4) {
         int var5 = var2[var4];
         System.out.print("" + var5 + "\n");
      }

      System.out.println();
   }

   public void inputNilaiBaru(int var1) {
      while(var1 > 0) {
         this.nilaiBaru.add(this.input.nextInt());
         --var1;
      }

   }

   public void cetakNilaiBaru() {
      ListIterator var1 = this.nilaiBaru.listIterator(0);

      while(var1.hasNext()) {
         Object var2 = var1.next();
         if (var2 == null) {
            System.out.println("null");
         } else {
            System.out.println(var2.toString());
         }
      }

   }
}
